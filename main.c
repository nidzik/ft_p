#include "libft/libft.h"

int main(void)
{
  char buf[256];
  int r;

  r = 0;
  ft_bzero(buf, 256);
  ft_putendl("avt");
  //  while(43)
  ((r = read(0, buf, 256)) );
    {
              execl("/bin/ls","~/",NULL);
      //ft_putendl("-___________-");
        ft_putendl(buf);
      //  break;
  
    }
  ft_putendl("apr");

  return(0);
}
